import { Button } from '@mui/material';
import React from 'react';

const Home = () => {
    return (
        <div>
            <h1>Test</h1>
            <Button variant='contained'>Home</Button>
        </div>
    );
};

export default Home;